# Used for bootstraping hub cluster with Addon Appset chart  
addons-appset: # for path routing
  enabled: true
  chartName: addons
  namespace: argocd
  releaseName: addons
  type: chart
  path: packages/appset-chart # This path is relative to Github repo root
  valueFiles: 
  - "path-routing-values.yaml" # this should be commented if not using path routing
  # selector:
  #   matchExpressions:
  #     - key: clusterName
  #       operator: In
  #       values: ['hub']
  #     - key: path_routing
  #       operator: In # this should be commented if not using path routing
  #       values: ['true']